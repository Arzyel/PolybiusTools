CURRENTLY KNOWN BUGS


1- inside mCultures there are incorrect tags. For exemple -> dynasty_names, male_names, female_names

dynasty_names is part of the evenks culture_group in the data,
female_names and male_names is part of the sudanese culture_group,

their position and style is in fact equivalent to a culture. and their culture_group is simply the 
last time in a culture_group they have appeared because of the id and the umap simply injecting it again at the same place.

PRIORITY : low

HOW TO FIX : revise the recursion adding the data. Either reconstruct it as loops like for religion or
if able to decode the recursion they go for that.